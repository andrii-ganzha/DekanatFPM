@model DekanatFPM.Models.Group

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Group</h4>
    <h3>@Html.ActionLink("Додати індивідуальний план", "AddIndividualPlan", new { groupID = Model.GroupID })</h3>
    <h3>@Html.ActionLink("Редагувати індивідуальний план", "EditIndividualPlan", new { groupID = Model.GroupID })</h3>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Specialization.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Specialization.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Type)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Type)
        </dd>
        <dt>Студенти</dt>

        <dd>
            @foreach (var s in ViewBag.Students)
            {
                <p>@s.Name</p>
            }
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.GroupID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
